        stage('Clear Docker Components') {
            steps {
                script {
                    COUNT = sh(script: "docker ps | grep imagename | wc -l", returnStdout: true).trim()
                    sh 'echo &COUNT'
                }
            }
        }
